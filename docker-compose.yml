services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: questhub
      POSTGRES_PASSWORD: questhubpassword
      POSTGRES_DB: questhub
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # backend:
  #   build: ./backend
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     POSTGRES_URL: postgres://questhub:questhubpassword@postgres:5432/questhub
  #     BETTER_AUTH_URL: http://localhost:3000
  #   depends_on:
  #     - postgres

volumes:
  postgres_data:
